+ Separate folder for gcloud App Engine to keep things simple.
+ Run `gcloud app deploy` from this folder to upload folder contents as a new build.
+ Require `gcloud init` beforehand.
+ Metadata specified in `app.yaml`.
